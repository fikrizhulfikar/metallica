var global = require('core-js/internals/global');
var setGlobal = require('core-js/internals/set-global');

var SHARED = '__core-js_shared__';
var store = global[SHARED] || setGlobal(SHARED, {});

module.exports = store;
