var has = require('core-js/internals/has');
var toIndexedObject = require('core-js/internals/to-indexed-object');
var indexOf = require('core-js/internals/array-includes').indexOf;
var hiddenKeys = require('core-js/internals/hidden-keys');

module.exports = function (object, names) {
  var O = toIndexedObject(object);
  var i = 0;
  var result = [];
  var key;
  for (key in O) !has(hiddenKeys, key) && has(O, key) && result.push(key);
  // Don't enum bug & hidden keys
  while (names.length > i) if (has(O, key = names[i++])) {
    ~indexOf(result, key) || result.push(key);
  }
  return result;
};
