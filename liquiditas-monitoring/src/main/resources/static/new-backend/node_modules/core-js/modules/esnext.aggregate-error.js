'use strict';
var $ = require('core-js/internals/export');
var DESCRIPTORS = require('core-js/internals/descriptors');
var getPrototypeOf = require('core-js/internals/object-get-prototype-of');
var setPrototypeOf = require('core-js/internals/object-set-prototype-of');
var create = require('core-js/internals/object-create');
var defineProperty = require('core-js/internals/object-define-property');
var createPropertyDescriptor = require('core-js/internals/create-property-descriptor');
var iterate = require('core-js/internals/iterate');
var createNonEnumerableProperty = require('core-js/internals/create-non-enumerable-property');
var InternalStateModule = require('core-js/internals/internal-state');

var setInternalState = InternalStateModule.set;
var getInternalAggregateErrorState = InternalStateModule.getterFor('AggregateError');

var $AggregateError = function AggregateError(errors, message) {
  var that = this;
  if (!(that instanceof $AggregateError)) return new $AggregateError(errors, message);
  if (setPrototypeOf) {
    that = setPrototypeOf(new Error(message), getPrototypeOf(that));
  }
  var errorsArray = [];
  iterate(errors, errorsArray.push, errorsArray);
  if (DESCRIPTORS) setInternalState(that, { errors: errorsArray, type: 'AggregateError' });
  else that.errors = errorsArray;
  if (message !== undefined) createNonEnumerableProperty(that, 'message', String(message));
  return that;
};

$AggregateError.prototype = create(Error.prototype, {
  constructor: createPropertyDescriptor(5, $AggregateError),
  message: createPropertyDescriptor(5, ''),
  name: createPropertyDescriptor(5, 'AggregateError')
});

if (DESCRIPTORS) defineProperty.f($AggregateError.prototype, 'errors', {
  get: function () {
    return getInternalAggregateErrorState(this).errors;
  },
  configurable: true
});

$({ global: true }, {
  AggregateError: $AggregateError
});
